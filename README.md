# Jarvis Reader

当前版本：v0.4.0

中文说明 | [English](./README.en.md)

Jarvis Reader 是一个面向 Obsidian 的个人化 EPUB 阅读插件。它把书架、目录导航、阅读进度、高亮标注、读书笔记和双链想法整合到同一个阅读流程里。

插件内置 ECDICT 离线词典，选中英文单词时可直接查询，无需导入词典或配置本地路径。词典数据按首字母分片加载，来源与许可见 [THIRD_PARTY_NOTICES.md](./THIRD_PARTY_NOTICES.md)。

## 版本记录

### v0.4.0

- 将 `index/word-assets.json` 设为词条唯一持久化主数据，停止从 `data.json` 合并恢复词条。
- 新增“同步词条到 Markdown”，支持缺失词条自动重建和彻底删除。
- 内置 400,850 条 ECDICT 离线词典，按首字母分片加载，无需导入或配置路径。
- 聚合词条笔记统一使用“单词 / 短语 / 句子”中文分区。

### v0.3.2

- 修复拖动改造引入的 JavaScript 语法错误，恢复插件正常加载。
- 恢复被误删的 EPUB 配置、阅读位置保存和高亮渲染等核心逻辑。
- 保留感想卡与翻译卡的指针捕获拖动优化。

### v0.3.1

- 实验性支持离线翻译功能，在划词翻译时可优先查询本地 JSON 词典，未命中时回退到 AI 翻译。
- 修复侧栏标注同步 bug，解决删除标注后被静默回读复活的问题。
- 精简优化词典设置项结构。

### v0.3.0

- 划词翻译会把选中文本所在句子传给提示词中的 `{{sentence}}`，优先生成符合上下文的释义。
- 支持短语按上下文翻译；短语保存后仍进入全局单词卡工作流。
- 自动区分单词、短语和句子：单词/短语生成单词卡，完整句子只显示中文译文，并提供“保存句子”。
- 实验性支持离线翻译功能，在划词翻译时可优先查询本地 JSON 词典，未命中时回退到 AI 翻译。
- 单词、短语和句子的原文下划线使用不同颜色区分，并在 Markdown 中分别写入 `Words`、`Phrases`、`Sentences` 分区。
- 聚合翻译笔记不再在每个条目下重复写 `Sources`，来源由文件名和 frontmatter 承担。
- 单词卡保存时同时记录 lemma、实际选中的词形和 EPUB CFI 来源，避免 `fracture/fractures` 这类词形变化导致原文不划线。
- 原始选中位置优先按 EPUB CFI 精确恢复蓝色单词下划线，其他位置再按已知词形自动扫描。
- 悬浮单词卡改为通过 EPUB 正文中的真实单词命中，不再依赖 SVG 底线命中，减少触发困难并避免破坏正文选区。
- 悬浮命中支持常见屈折词形映射，例如 `fractures -> fracture`、`shattered -> shatter`。
- 新增插件目录 sidecar 索引副本，保存标注和词卡定位，降低 `data.json` 被覆盖后丢失链接索引的风险。
- 阅读器外层背景跟随 Obsidian 明暗主题，修复深色模式下正文外侧仍为浅色的问题。
- 普通高亮改为更醒目的荧光笔黄，有想法标注改为更清晰的橙色描边。
- 新生成的单词卡不再写空的 `## Thoughts` 区块。

### v0.2.0

- 新增划词翻译和全局单词卡工作流：选中 EPUB 单词后可翻译、保存、重新加入、标记已掌握。
- 单词卡正文采用 `display` 优先机制，翻译弹窗、悬浮词卡和 Markdown `## Card` 共用同一份正文显示源。
- 单词笔记写入格式简化为 `## Card` 和 `## Sources`，减少重复字段和历史元数据噪音。
- 翻译提示词设置改为中文说明，支持恢复默认提示词，并在测试前校验 JSON 示例和必需字段。
- 单词卡支持从 Markdown `## Card` 回读完整正文，并对显示缓存增加数量和长度保护。
- 悬浮词卡右上角提供 Obsidian 风格图标操作：已掌握、删除词条、打开词条。
- 删除词条会同时删除 Markdown 中对应词条块和插件索引。
- 翻译弹窗点击红色单词标题即可发音，保存结果使用 Obsidian Notice 提示。
- 点击 EPUB 正文空白处会关闭翻译弹窗，减少残留浮窗干扰阅读。

### v0.1.7

- 感想编辑器中的 `[[双链]]` 候选更接近 Obsidian 行为，优先显示最近打开和最近修改的文件。
- 双链候选支持 Markdown 之外的库内文件，例如 EPUB、PDF、DOC、DOCX，并忽略 `.obsidian` 等隐藏目录。
- `#`、`^`、`|` 的提示文案与 Obsidian 链接输入保持一致。
- 感想弹窗默认位置改为右侧中部，双链候选列表改为弹窗内部布局，避免遮挡保存按钮。
- 默认读书笔记模板新增 `created` 字段，`bookname` 默认使用双链格式。

## 来源与致谢

Jarvis Reader 是基于 [Awesome Reader](https://github.com/awesomedog/obsidian-awesome-reader) 的个人化改造版本。插件的基础阅读能力、EPUB 阅读框架和部分结构来自 Awesome Reader。

本仓库主要记录我围绕个人读书流做的改造，包括书架与目录布局、标注侧栏、高亮与想法、Obsidian 双链输入、阅读进度显示和界面细节调整。

上游 Awesome Reader 由 awesomedog 创建，并采用 MIT 许可证。若继续公开发布或分发本插件，应保留对原项目的署名与许可证说明。

## 功能

- 在 Obsidian 中直接打开 `.epub` 文件。
- 通过 Jarvis Reader 书架浏览 EPUB 书籍。
- 在书架、目录、标注面板之间切换。
- 支持分页阅读和连续滚动阅读。
- 支持单页和双页阅读模式。
- 按书籍记录阅读进度。
- 显示章节内页码和全书阅读百分比。
- 选中正文后创建高亮，或继续写想法。
- 将高亮原文、想法和块标识符写入同名 Markdown 读书笔记。
- 有想法的高亮在阅读区使用更明显的视觉标记。
- 在想法中使用 Obsidian 风格的 `[[双链]]`。
- 在标注预览中点击双链打开对应笔记。
- 选中英文单词或短语后生成翻译，并保存为全局单词卡。
- 翻译时可结合所在句子优先解释当前语境中的含义。
- 单词卡支持悬浮预览、发音、打开词条、标记已掌握和删除。
- 已保存单词会在原文中显示蓝色下划线，并支持常见词形变化的悬浮卡片命中。
- 已保存短语和句子也会在原文中显示对应类型的下划线。
- 单词卡内容以 Markdown `## Card` 写入笔记，减少空区块和重复来源信息。
- 插件会在本地 sidecar 文件中保存标注和词卡定位索引，便于从 `data.json` 覆盖或丢失中恢复。
- 在侧边栏按全部、高亮、想法筛选标注，并通过更多菜单筛选当前章节、有链接或切换排序。
- 单击侧边栏标注定位到正文对应位置，双击打开想法编辑窗口。
- 写入读书笔记的时间使用本地时间格式。

## 阅读进度

Jarvis Reader 使用分层进度机制：

1. 如果 EPUB 自带页表，优先显示真实全书页码。
2. 如果 EPUB 没有页表，则显示当前章节页码和全书百分比。
3. 全书百分比优先使用 EPUB locations。
4. 如果 locations 不可用，则用章节位置和章节内页码估算。

这样可以避免把“章节内页码”误当成“全书页码”。

## 安装

手动安装：

1. 下载或克隆本仓库。
2. 将整个文件夹复制到 Obsidian 仓库：

```text
.obsidian/plugins/jarvis-reader
```

3. 确认文件夹中至少包含：

```text
main.js
manifest.json
styles.css
```

4. 打开 Obsidian。
5. 进入 `设置 -> 第三方插件`。
6. 重新加载插件列表。
7. 启用 `Jarvis Reader`。

## 使用

- 点击 Obsidian 左侧功能区的 Jarvis Reader 图标打开书架。
- 从书架打开 EPUB。
- 阅读时选中文本，可选择 `高亮` 或 `写想法`。
- 普通高亮只保存原文；写想法会保存原文和你的感想。
- 选中英文单词或短语后点击 `翻译`，可生成上下文优先的单词卡。
- 选中完整句子后点击 `翻译`，只显示整句中文译文。
- 保存后的翻译卡片会按单词、短语、句子分区写入同一本书的词汇笔记。
- 在想法中输入 `[[笔记名]]`，把读书想法连接到已有知识库。
- 可在插件设置中配置读书笔记文件夹和笔记模板。

## 数据与隐私

本仓库应只包含插件代码：

```text
main.js
manifest.json
styles.css
README.md
README.en.md
```

阅读进度、高亮标注、封面缓存、插件设置等本地数据可能由 Obsidian 存储在 `data.json` 中。该文件已被 `.gitignore` 排除，不应提交到 GitHub。

## 开发说明

当前仓库保存的是插件构建后的文件，不是原始源码工程。`main.js` 是打包后的文件。

提交前建议执行：

```powershell
node --check main.js
```

推荐的 Obsidian 验证命令：

```powershell
obsidian plugin:reload id=jarvis-reader
obsidian dev:errors
```
